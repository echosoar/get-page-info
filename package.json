{
  "name": "page-info",
  "version": "0.0.1",
  "description": "get web page info",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "esbuild src/* --outdir=dist --platform=node --format=cjs --watch",
    "build": "rm -rf dist && esbuild src/* --outdir=dist --platform=node --format=cjs",
    "test": "mw test",
    "coverage": "jest --coverage",
    "clean": "rm -rf node_modules/ package-lock.json dist"
  },
  "devDependencies": {
    "@midwayjs/cli": "^1.2.93",
    "@types/cheerio": "^0.22.30",
    "@types/jest": "^27.4.0",
    "@types/node": "^10.17.60",
    "esbuild": "^0.13.8",
    "jest": "^29.2.2",
    "ts-jest": "^29.0.3",
    "typescript": "^4.1.0"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "cheerio": "^1.0.0-rc.10",
    "iconv-lite": "^0.6.3",
    "numto": "^0.1.1",
    "p-limit": "^3.0.0"
  },
  "files": [
    "bin",
    "dist"
  ],
  "bin": {
    "dev": "./bin/dev.js",
    "d": "./bin/dev.js"
  },
  "author": "echosoar",
  "license": "MIT"
}
